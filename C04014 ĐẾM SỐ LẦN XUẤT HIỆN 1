#include <stdio.h>
 int main() {
  int n;
   scanf("%d",&n);
    int a[n],b[100]={0};
    for(int i=0;i<n;i++) { 
	scanf("%d",&a[i]); b[a[i]]++; }
	 for(int t=0;t<n;t++) {
	  if(b[a[t]]>=1)
	   { 
	   printf("%d %d\n",a[t],b[a[t]]);
	    b[a[t]]=0; } } }
	    
