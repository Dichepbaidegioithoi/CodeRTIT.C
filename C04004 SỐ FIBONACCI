#include <stdio.h>
int main()
{
	int test;
	scanf("%d", &test);
	while(test--)
	{
		long long a[100]={1,1};
		for(int i=2;i<=100;i++)
		{
			a[i]=a[i-1]+a[i-2];
		}
		int i;
scanf("%d", &i);
			 printf("%ld\n", a[i-1]);

	 } 	
	 return 0;
}
