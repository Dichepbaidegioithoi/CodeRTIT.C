#include <stdio.h>
#include <math.h>
int main()
{
	int i,a,n;
	scanf("%d%d", &a, &n);
	int s;
	s=0;
	if(a<=n)
	{
	for (int i=a;i<=n;i++)
	{
		s=s+i;
	}
}else
	{
		for(int i=n;i<=a;i++)
		{
			s= s+i;
		}
	}
		printf("%d",s);
	return 0;
}
