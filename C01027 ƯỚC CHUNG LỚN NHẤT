#include <stdio.h>
#include <math.h>
int main()
{ 
int test;
scanf("%d", &test);
while(test--)
{
	int a,b;
	scanf("%d %d", &a, &b);
	while(a!=b)
	{
		if(a>b)
		{
			a-=b;
		}else
		{
			b-=a;
		}
	}
	printf("%d\n",a);
}
return 0;
}
