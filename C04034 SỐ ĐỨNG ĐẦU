#include<stdio.h>
 int main(){
  int t;
   scanf("%d",&t);
    while(t--){ 
	int n;
	 scanf("%d",&n);
	  int i,j,a[n];
	   for(i=0;i<n;i++){
	    scanf("%d",&a[i]); }
		 for(i=0;i<n-1;i++){ 
		 int tam =1 ;
		  for(j=i+1;j<n;j++){ 
		  if(a[i] <= a[j]){ 
		  tam = 0;
		   break; 
		   } } 
		   if(tam == 1) 
		   printf("%d ",a[i]); } 
		   printf("%d\n",a[n-1]); }
		    return 0; }

